# Synthetic manuscript for a reproducible editing demo

## Introduction

We use recent local traffic observations to adjust traversal costs during online planning.

It is worth noting that the method works in some cases. We do not claim that it applies to every setting. These findings should be interpreted with caution.

## Method

We use recent local traffic observations to adjust traversal costs during online planning.

The update window is NEEDS REAL VALUE steps. This value determines which observations enter the cost update.

## Results

| Method | Throughput (tasks/min) | Runtime (ms) |
| --- | --- | --- |
| Baseline | 100.00 | 8.2 |
| Proposed | 112.000000 | 8.7 |

The shuffled-input control results are inconclusive. We cannot draw robust conclusions about the contribution of traffic information. Nevertheless, the method reliably exploits the information in the shared trace.

## References

This section is synthetic and contains no citations.
